import Placeholder from '@/components/Placeholder';

export default function RawData() {
  return (
    <Placeholder
      title="Raw Data"
      blurb="Sortable, filterable table of every R50 shot with session filters and the In-Model / Excluded flags."
    />
  );
}
