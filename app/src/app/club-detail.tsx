import Placeholder from '@/components/Placeholder';

export default function ClubDetail() {
  return (
    <Placeholder
      title="Club Detail"
      blurb="Per-club headline cards, carry histogram, dispersion scatter, consistency bars, and the shot-by-shot table will live here."
    />
  );
}
